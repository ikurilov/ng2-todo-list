import { TodoListFilterPipe } from './todo-list-filter.pipe';

describe('TodoListFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new TodoListFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
